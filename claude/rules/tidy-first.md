# Tidy Firstアプローチ

- すべての変更を2種類に明確に分離する：
  1. 構造的変更：振る舞いを変えずにコードを整理する（リネーム、メソッドの抽出、コードの移動）
  2. 振る舞いの変更：実際の機能を追加・修正する
- 同じコミットに構造的変更と振る舞いの変更を混在させない
- 両方が必要な場合、常に構造的変更を先に行う
- 構造的変更が振る舞いを変えていないことを、変更前後にテストを実行して検証する
